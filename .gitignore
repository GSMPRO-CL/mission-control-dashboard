node_modules/
.env
.DS_Store
.next/

# Local working copies / secrets (never commit)
Working Folder/

# Herramientas de IA — si vuelven a dashboard/, ignorarlas (deben estar en docs/.agents/)
dashboard/.agents/
dashboard/.mcp.json
dashboard/skills-lock.json

# Secrets
google_analytics.json

env.yaml
